import React from 'react';
import { useDispatch } from 'react-redux';

const Home = () => {
    const dispatch = useDispatch();

    // camera api
    // dispatch
    return (
        <div>
            Home
        </div>
    );
};

export default Home;